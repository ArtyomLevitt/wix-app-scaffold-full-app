# README Template — Wix CLI App

Every generated Wix CLI app MUST include a `README.md` following this exact structure.
Adapt the content to match the specific app being built.

---

## Template

```markdown
# {App Title} – Wix App

{One-sentence description of what the app does.}

## Features

- **{Feature 1}** – {Short description}
- **{Feature 2}** – {Short description}
- **{Feature 3}** – {Short description}
- **Premium plan support** – Free plan allows {limit}; premium unlocks {expanded capability}
- **Multi-language dashboard** – {N} languages supported ({language codes})
- **Event tracking** – Tracks app install, removal, and plan changes via Supabase

## Project Structure

src/
├── backend/
│   ├── _shared/
│   │   ├── supabase-client.ts       # Supabase client for analytics
│   │   └── tracking.web.ts          # Setup-completed tracking
│   ├── events/
│   │   ├── app-installed/           # App install event handler
│   │   ├── app-removed/             # App removal event handler
│   │   └── plan-changed/            # Plan upgrade/downgrade handler
│   ├── check-premium.web.ts         # Premium status web method
│   ├── app-plans.web.ts             # App pricing plans
│   └── {app-specific-backend}.ts    # App-specific backend logic
├── dashboard/
│   ├── _shared/
│   │   └── rate-popup.ts            # Review popup custom element
│   └── pages/
│       ├── page.tsx                 # Main dashboard UI
│       ├── page.json                # Dashboard page manifest
│       └── assets/                  # App icons and images
├── intl/                            # (if i18n enabled)
│   ├── load-messages.ts             # Dynamic locale message loader
│   ├── withIntlProvider.tsx          # react-intl HOC wrapper
│   └── messages/                    # Translation JSON files
└── site/
    └── {widgets|embedded-scripts|plugins}/
        └── {component-name}/
            ├── {entry-file}         # Main component
            ├── {manifest}.json      # Component manifest
            └── panel.tsx            # Settings panel (if widget)

## How It Works

1. **Dashboard** – Site owner configures {what} through the dashboard page
2. **Storage** – Settings are saved to {where — Wix Data collection, Supabase, etc.}
3. **{Site component type}** – {How the site-side component works}
4. **{Key mechanism}** – {How the core functionality operates}

## Development

# Install dependencies
npm install

# Start local dev server
npm run dev

# Type-check
npm run typecheck

# Build for production
npm run build

# Release new version
npm run release

## Tech Stack

- **Wix CLI** (@wix/cli-app) – App framework and build tooling
- **React 16** – Dashboard UI
- **Wix Design System** (@wix/design-system) – UI components
- **react-intl** – Internationalization (if i18n enabled)
- **Supabase** – Analytics and event tracking
- {Any additional tech specific to this app}
```

---

## Rules

1. The project structure tree MUST reflect the actual generated files — do NOT include files that don't exist
2. Keep feature descriptions concise (one line each)
3. "How It Works" should be 3–5 numbered steps explaining the data flow
4. Always include the Development section with all 5 commands
5. The Tech Stack should list only actually-used dependencies
6. Do NOT include GitHub metadata (stars, watchers, forks, contributors, languages) — that's auto-generated by GitHub
